${packageName}
-----

[![Build Status](https://circleci.com/gh/tippenein/${packageName}.svg?style=shield&circle-token=<TOKEN HERE>)](https://circleci.com/gh/tippenein/${packageName})

``` sh
# Configure & build the package.
stack build

# Test package.
stack test

# install to ~/.local/bin
stack install


# Start REPL.
stack repl
```
